{{- /* Custom sitemap template.
     Improves on Hugo's built-in by applying per-kind changefreq and
     priority values and using the git-based lastmod date when available.
*/ -}}
{{- $pages := .Data.Pages -}}
{{ "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- range $pages }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02" }}</lastmod>
    {{- end }}
    {{- with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>
    {{- else }}
    <changefreq>{{ if .IsHome }}daily{{ else if eq .Kind "section" }}weekly{{ else if eq .Type "blog" }}monthly{{ else }}weekly{{ end }}</changefreq>
    {{- end }}
    <priority>{{ if ge .Sitemap.Priority 0.0 }}{{ .Sitemap.Priority }}{{ else if .IsHome }}1.0{{ else if eq .Kind "section" }}0.8{{ else if eq .Type "blog" }}0.7{{ else }}0.5{{ end }}</priority>
  </url>
  {{- end }}
</urlset>
